# แผนการวัดวัตถุประสงค์ ฉบับใช้งานจริง

ปรับปรุง 24 ก.ย. · แทนฉบับเดิมทั้งหมด · ทำทีละรอบ ไม่ต้องเปิดทุกอย่างพร้อมกัน

---

## 0. ภาพรวม — อ่านหน้านี้หน้าเดียวก็พอ

| ข้อ | วัดอะไร | เกณฑ์ | สถานะ | ต้องทำอะไร | รอบ |
|---|---|---|---|---|---|
| 1 | ความคลาดเคลื่อนตำแหน่งหุ่นบนแผนที่ | ≤ 10 ซม. | ❌ ยังเกิน (เช็ก 24 ก.ย.: ALIGNED 42.4 ซม.) | **ต้องทำรอบ D** | D |
| 2 | ความแม่นยำการตรวจจับ (macro F1) | ≥ 80% | 🟡 prelabel แล้ว รอตรวจแก้ label (ใช้ชุดมุมมองเดียว D-27) | labelImg `~/eval/insitu2_stationary` → score | A + โต๊ะ |
| 3 | ความเร็วตรวจจับ | ≥ 5 FPS | ✅ **ผ่าน** 13.05 FPS | ไม่ต้องทำอะไร | — |
| 4 | ความคลาดเคลื่อนตำแหน่งวัตถุบนแผนที่ | ≤ 50 ซม. | 🟡 ผ่านบางส่วน 8 ซม. | วัด 4 ด้านรอบเก้าอี้ | B |
| 5 | CPU เฉลี่ยขณะทำงานเต็มระบบ | ≤ 80% | ⬜ ยังไม่วัด | เก็บค่าขณะขับ | C |

### แต่ละรอบต้องเปิดอะไร

| รอบ | ใช้วัด | `real` | `slam` | `yolo` | `semantic` | `nav` | `bag` | เวลา |
|---|---|:-:|:-:|:-:|:-:|:-:|:-:|---|
| **A** | ข้อ 2 (อัดภาพ) | ✅ | | ✅ | | | ✅ | 15 นาที |
| **B** | ข้อ 4 + แคปรูป 3 รูป | ✅ | ✅ | ✅ | ✅ | | | 20 นาที |
| **C** | ข้อ 5 | ✅ | ✅ | ✅ | ✅ | | ❌ ห้าม | 10 นาที |
| **D** | ข้อ 1 | ✅ | ✅ | | | | | 40 นาที |
| **E** | รูป Nav2 | ✅ | | | | ✅ | | 5 นาที |

ทุกรอบเปิด `make teleop SPEED=0.10` ไว้ขับด้วย · รอบ B กับ C ต่อกันได้เลยเพราะเปิดชุดเดียวกัน

**ถ้าเวลาน้อย ทำตามลำดับนี้:** เช็กข้อ 1 ที่โต๊ะ → A → B+C → D → E → งานที่โต๊ะ

---

## 1. ก่อนออกสนาม

### 1.1 เช็กข้อ 1 จากผลเดิม (1 นาที ไม่ต้องเปิดหุ่น)

```bash
ros2 run my_bot slam_accuracy_check.py --summary
```

ถ้า `ALIGNED` ผ่าน 10 ซม. → ใช้ผลเดิมได้ ข้ามรอบ D (ดูหัวข้อ 5.4 ว่าเขียนรายงานยังไง)

> **ผลเช็ก 24 ก.ย.:** 27 ครั้ง / 8 จุด · ABSOLUTE 104.5 ซม. · **ALIGNED 42.4 ซม.** (MARK_B) ·
> REPEATABLE 90.2 ซม. (HOME 14 ครั้ง — ไฟล์เก่าน่าจะปนหลาย session) · scale −3.98% → **ไม่ผ่าน ต้องทำรอบ D**

### 1.2 ของที่ต้องเตรียม

- [ ] ตลับเมตร · เทปติดพื้น · เชือกยาว 3–4 ม.
- [ ] เก้าอี้ 3–4 ตัว · กระเป๋าเป้ 2–3 ใบ · โน้ตบุ๊ก 1–2 เครื่อง
- [ ] คน 2–3 คนที่**ยินยอม**ให้ถ่ายภาพ
- [x] `git pull` บน Jetson ให้ได้ `detection_accuracy.py` ตัวที่มีคำสั่ง `prelabel` (24 ก.ย. `65f9132`)

### 1.3 วิธีเปิดระบบ (ใช้ทุกรอบ)

เปิดทีละ terminal บน Jetson **ตามลำดับ** รอให้ตัวก่อนหน้าขึ้นครบก่อนเปิดตัวถัดไป

| คำสั่ง | รอจนเห็น |
|---|---|
| `cd ~/cap_ws && make real` | `controller_manager` ขึ้นครบ ไม่มี error สีแดง |
| `make slam` | `Registering sensor` แล้วนิ่ง |
| `make yolo` | เฟรมเรตไหลต่อเนื่อง |
| `make semantic` | `semantic_mapper` พร้อมทำงาน |
| `make teleop SPEED=0.10` | ใช้ขับหุ่น |

เปิดเฉพาะแถวที่รอบนั้นใช้ (ดูตารางหัวข้อ 0) แล้วเปิด terminal อีกอันไว้พิมพ์คำสั่งวัด

---

## 2. รอบ A — อัด bag สำหรับข้อ 2

**เปิด:** `real` → `yolo` → `teleop` · **ไม่ต้อง** slam / semantic

### 2.1 จัดฉาก

| คลาส | จัดยังไง |
|---|---|
| `person` | คน 2–3 คนเดินผ่าน/ยืนในฉาก — **ขอความยินยอมก่อน** |
| `chair` | เก้าอี้ 3–4 ตัว หลายแบบ กระจายในพื้นที่ |
| `backpack` | กระเป๋าเป้ 2–3 ใบ **วางบนพื้น** |
| `laptop` | โน้ตบุ๊กกาง 1–2 เครื่อง **วางต่ำ** (เก้าอี้เตี้ย/พื้น) |

- ให้มีฉากโล่ง (ทางเดิน กำแพง) ปนประมาณ 20–30% เพื่อทดสอบการตรวจจับผิด
- ถ่ายหลายระยะ (1–4 ม.) หลายมุม วัตถุชิดขอบภาพ และโดนบังบางส่วน

### 2.2 อัด

รอบนี้ไม่เปิด `semantic` จึง**ไม่มี** `/image/compressed` (และถึงเปิด มันก็เป็นภาพที่ yolo วาดกรอบทับแล้ว
ใช้ label ไม่ได้) → เปิด republish จากภาพกล้องดิบเองก่อนอัด แล้วอัดแค่ 2 topic:

```bash
# terminal 1 — ภาพกล้องดิบ /image → /image/compressed (stamp เดียวกับ /detections)
source /opt/ros/humble/setup.bash
ros2 run image_transport republish raw compressed --ros-args \
    -r in:=/image -r out/compressed:=/image/compressed

# terminal 2
ros2 topic hz /image/compressed      # ต้องได้ ~15 Hz ก่อนอัด
make bag TOPICS="/image/compressed /detections"
```

ขับวนช้า ๆ **อย่างน้อย 4 นาที** อย่าจอดนิ่ง (ได้ภาพซ้ำ ตัวเลขดีเกินจริง) → Ctrl+C หยุด

### 2.3 ⚠️ ลอง extract ทันที ก่อนเก็บของ

```bash
cd ~/cap_ws/src/my_bot/scripts
source /opt/ros/humble/setup.bash && source ~/cap_ws/install/setup.bash
python3 detection_accuracy.py extract --bag ~/bags/<ชื่อ-bag-ใหม่> --out ~/eval/insitu2 --every 23 --matched-only
```

ดู 2 อย่าง:
- `detections matched ... N / N frames` ต้องครบ ถ้าไม่ครบ = เฟรมที่ yolo ไม่ได้ประมวลผล (yolo ~13 FPS < กล้อง 15 Hz)
  → ลบโฟลเดอร์ out แล้วรันใหม่ด้วย `--matched-only` · **อย่าใช้ `--slop 0.1`** (จะเอากรอบของเฟรมข้าง ๆ ห่าง 66 ms มาใส่)
  · 24 ก.ย. bag `2026-09-24-165025`: 3283/3962 ภาพถูกประมวลผล → 143 เฟรม ครบ 143/143
- ตารางคลาสท้ายผล — `backpack` กับ `laptop` ต้องไม่ใช่หลักหน่วย ถ้าน้อย → อัดเพิ่มตอนนี้เลย
- **เปิดดูภาพใน `frames/` ห่าง ๆ กัน 5–6 ภาพ ฉากหลังต้องเปลี่ยน** ถ้าโน้ตบุ๊ก/กระเป๋าอยู่ที่เดิมทุกภาพ = หุ่นไม่ได้ขยับ
  → จำนวน instance ที่ดูเยอะคือของชิ้นเดียวซ้ำ 100 กว่าครั้ง ใช้ไม่ได้ อัดใหม่ (เกิดจริง 24 ก.ย. ดู `records/objective-tests.md`)

> bag ใหม่บีบอัดด้วย zstd — 24 ก.ย. ลองแล้ว **พัง** (`file is not a database`) แก้แล้วใน `65f9132`
> ทดสอบกับ bag สังเคราะห์ทั้ง zstd และไม่บีบอัดผ่าน แต่ยังควรลองกับ bag จริงทันทีเหมือนเดิม
> extract จะแตก `*.db3` ไว้ข้าง `*.db3.zstd` ใน `~/bags/` (ใช้ดิสก์เพิ่มเท่าขนาดเต็ม)

**งานที่เหลือของข้อ 2 ทำที่โต๊ะ → หัวข้อ 7**

---

## 3. รอบ B — ข้อ 4 วัดตำแหน่งเก้าอี้ 4 ด้าน

**เปิด:** `real` → `slam` → `yolo` → `semantic` → `teleop`

**เช็กก่อนเริ่ม:**

```bash
ros2 topic hz /semantic_landmarks
ros2 run tf2_ros tf2_echo map base_footprint
```

ถ้าคำสั่งไหนไม่มีข้อมูลหรือ error อย่าเพิ่งวัด — ย้อนดูว่า terminal ไหนตาย

### 3.1 วัดตลับเมตร 2 ค่า

```
                    วัตถุ (เก้าอี้)
                      ●
                      |
                      |  ← left = ระยะตั้งฉาก
                      |    (ซ้าย = บวก, ขวา = ลบ)
      [หุ่นยนต์]------+------------→ แนวกึ่งกลางลำตัว
        ▲
    กึ่งกลางเพลาล้อ
        |←─ fwd ─→|
```

- **`--fwd`** วัดตามแนวกึ่งกลางลำตัว จาก**กึ่งกลางเพลาล้อ** (ไม่ใช่หัวหุ่น)
- **`--left`** วัดตั้งฉากออกไปหาวัตถุ · ซ้าย = บวก · **ขวา = ลบ**
- หาแนวกึ่งกลางด้วยเชือกทาบ อย่ากะด้วยตา (เอียง 3° ที่ 1.5 ม. = คลาด 8 ซม.)
- ถ้าวัตถุเฉียงมาก วัดระยะตรง `d` กับมุม `θ` แล้วคิด `fwd = d·cosθ`, `left = d·sinθ`

### 3.2 วัด 4 ด้าน

**ห้ามขยับเก้าอี้ตลอดรอบนี้** · วัดด้าน `front` ใหม่ด้วย ให้ทั้ง 4 ค่ามาจากสคริปต์และวิธีเดียวกัน

| ด้าน | จอดยังไง | `--pass-label` |
|---|---|---|
| 1 | หน้าเก้าอี้ ห่าง ~1.5 ม. หันเข้าหา | `front` |
| 2 | อ้อมไปด้านขวา หันเข้าหา | `right` |
| 3 | อ้อมไปด้านหลัง หันเข้าหา | `back` |
| 4 | อ้อมไปด้านซ้าย หันเข้าหา | `left` |

**แต่ละด้าน:** จอด → รอนิ่ง 3 วินาที → วัดตลับเมตร → เช็กว่า yolo เห็นเก้าอี้ → รันคำสั่ง → **ห้ามขยับหุ่น 30 วินาที**

```bash
ros2 run my_bot object_accuracy.py chair --fwd <ค่าจริง> --left <ค่าจริง> --pass-label front
ros2 run my_bot object_accuracy.py chair --fwd <ค่าจริง> --left <ค่าจริง> --pass-label right
ros2 run my_bot object_accuracy.py chair --fwd <ค่าจริง> --left <ค่าจริง> --pass-label back
ros2 run my_bot object_accuracy.py chair --fwd <ค่าจริง> --left <ค่าจริง> --pass-label left
ros2 run my_bot object_accuracy.py chair --summary
```

### 3.3 อ่านผล

แต่ละด้านดู 2 บรรทัด: **`ERROR` ≤ 50 ซม.** · **`spread`** ยิ่งน้อยยิ่งดี

`--summary` จะบอกความหมายของการกระจายระหว่าง 4 ด้าน:

| สคริปต์บอกว่า | แปลว่า | เขียนในรายงาน |
|---|---|---|
| **CONVERGENCE** (id เดิม) | วัตถุชิ้นเดิม ตำแหน่งปรับเข้าที่ | ผสานข้อมูลข้ามมุมมองได้จริง |
| **VIEWPOINT DEPENDENCE** (id เปลี่ยน) | ระบบสร้างวัตถุใหม่แทนการผสาน | ข้อจำกัด รายงานตรง ๆ |

บรรทัดขึ้นต้นด้วย `!` = มี landmark ซ้ำของวัตถุเดียวกัน → จดไว้ เป็นข้อจำกัด

### 3.4 ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ | แก้ |
|---|---|---|
| `no landmark of class 'chair'` | กล้องไม่เห็น | ขับเข้าใกล้ เช็กหน้าจอ yolo |
| ERROR > 1 ม. | ใส่ `--left` ผิดเครื่องหมาย | ขวาต้องเป็นลบ |
| ERROR โตขึ้นทุกด้าน | ตำแหน่งหุ่นบนแผนที่เพี้ยน (ข้อ 1) | จดไว้ วิเคราะห์ในรายงาน |
| `spread` ใหญ่ทั้งที่จอดนิ่ง | หุ่นยังไม่นิ่ง / วัตถุชิดขอบภาพ | รอเพิ่ม 3 วินาที รันใหม่ |

### 3.5 แคปรูประหว่างระบบเปิดครบ (~5 นาที)

| ไฟล์ | วิธี |
|---|---|
| `all_nodes_running.png` | `ros2 node list` → แคปหน้าต่าง terminal |
| `tf_tree.png` | `cd ~ && ros2 run tf2_tools view_frames` → `pdftoppm -png -r 200 -singlefile frames_*.pdf tf_tree` |
| `ros2_node_graph.png` | `rqt_graph` → เลือก **Nodes/Topics (active)** → ติ๊ก Hide ทั้งหมด → 🔄 → Save as image |

`tf_tree` ต้องเห็นสาย `map → odom → base_footprint → base_link` ครบ

---

## 4. รอบ C — ข้อ 5 CPU

**เปิด:** ชุดเดียวกับรอบ B · **ห้ามอัด bag** (zstd กิน CPU) · **ห้ามเปิด RViz/เบราว์เซอร์บน Jetson**

### 4.1 เก็บ 3 ช่วง

```bash
ros2 run my_bot resource_report.py --seconds 120 --label "idle"                 # ก่อนเปิดอะไร
ros2 run my_bot resource_report.py --seconds 180 --label "real+slam"            # ยังไม่เปิด yolo
ros2 run my_bot resource_report.py --seconds 300 --label "full stack, driving"  # เปิดครบ + ขับตลอด
ros2 run my_bot resource_report.py --summary
```

> ถ้าจะทำต่อจากรอบ B ให้เก็บแค่ช่วง `full stack, driving` ก็พอ ส่วน idle / real+slam เก็บตอนเปิดระบบรอบไหนก็ได้

ระหว่างช่วง full stack รันอีก terminal (เผื่อต้องรู้ว่าใครกิน CPU):

```bash
top -b -n 1 -o %CPU | head -20
```

`%CPU` ของ `top` คิดต่อ 1 แกน — 100% = เต็ม 1 แกน

### 4.2 อ่านผล

| บรรทัด | ความหมาย |
|---|---|
| `CPU mean (all 6 cores)` | **ตัวเลขหลัก** เทียบ 80% |
| core ที่สูงสุด | ถ้า > 90% แม้ค่าเฉลี่ยผ่าน → เขียนประโยคเรื่องคอขวดแกนเดียวด้วย |
| GPU / RAM / tj | ใส่ย่อหน้าผล · tj ต้องไม่ใกล้ 97 °C |

### 4.3 ถ้าเกิน 80%

1. เช็กว่าไม่ได้อัด bag / เปิด RViz / เปิดเบราว์เซอร์บน Jetson อยู่
2. ดู `top` ว่าใครกิน แล้วแก้ตรงจุด:

| ตัวที่กินหนัก | แก้ |
|---|---|
| `python3` ของ yolo_detector | ลดความถี่ตรวจจับเหลือ 8–10 FPS (เกณฑ์ข้อ 3 ขอแค่ 5) |
| `slam_toolbox` | ปรับ `throttle_scans` / `minimum_travel_distance` |
| `ros2 bag` / `cam2image` | ปิด bag ระหว่างวัด |

3. ปรับอะไรต้องเขียนในรายงาน · **ห้ามเลือกเฉพาะช่วงที่สวย**
4. แก้ไม่ได้ → รายงานไม่ผ่าน พร้อมตารางแยกชั้นว่าใครกินเท่าไหร่

> เปลี่ยนเป็น TensorRT **ไม่ใช่ทางแก้แรก** — ช่วยฝั่ง GPU เป็นหลัก ส่วน CPU ของโหนด yolo
> ส่วนใหญ่คือแปลงภาพ เตรียมภาพ และตัวติดตามวัตถุ ซึ่ง TensorRT ไม่ได้ช่วย

---

## 5. รอบ D — ข้อ 1 ความคลาดเคลื่อนตำแหน่งหุ่น

**ทำเฉพาะถ้าหัวข้อ 1.1 ไม่ผ่าน** · **เปิด:** `real` → `slam` → `teleop` · ไม่ต้อง yolo / semantic

### 5.1 หลักการ (อ่านครั้งเดียวพอ)

- `make slam` ตั้งตำแหน่งหุ่นตอนเริ่มเป็น (0,0) · x = ทางหน้าหุ่น · y = ทางซ้าย
- ติดกากบาทจุดอ้างอิงที่พื้น วัดด้วยตลับเมตรว่าอยู่ห่าง HOME เท่าไหร่ = **truth**
- จอดทับกากบาท → สคริปต์อ่านตำแหน่งที่ SLAM บอก เฉลี่ย 5 วินาที
- **error = ระยะห่างระหว่างสองตำแหน่งนี้** เช่น truth (3.00, 0.00) · SLAM บอก (3.04, −0.03) → error = √(0.04² + 0.03²) = 5 ซม.
- แต่ละจุดเทียบกับ truth ของตัวเอง **error ไม่ได้บวกต่อกันจากจุดก่อนหน้า**
- SLAM ทาบสแกนไลดาร์กับผนังทุกสแกน error จึงไม่ควรไต่ตามรอบเหมือน odometry
  ถ้าไต่ขึ้นทุกรอบ = ขับเร็วไป / ล้อลื่น

### 5.2 เตรียมพื้นที่ (ก่อนเปิด slam)

1. ย้ายไฟล์ผลเก่าออก (**อย่าลบ** เผื่อต้องใช้เทียบ):
   ```bash
   mv ~/maps/slam_accuracy.jsonl ~/maps/slam_accuracy_old.jsonl
   ```
2. ติดกากบาท **HOME** · ขึงเชือก 3–4 ม. เป็นแนวแกน x
3. จอดหุ่นทับ HOME **กึ่งกลางหุ่นทาบแนวเชือก**
4. ติดกากบาทจุดอื่น 3–4 จุด **กระจายทั่วพื้นที่ 5×5 ม.**
5. วัด truth ของ**ทุกจุดจาก HOME / เส้นเชือกโดยตรง** — ห้ามวัดต่อกัน A→B (ตลับเมตรคลาดสะสม)
   x = ตามแนวเชือก · y = ตั้งฉาก ซ้ายบวก ขวาลบ
6. แล้วค่อย `make real` → `make slam` → `make teleop SPEED=0.10`

### 5.3 วัด

```bash
ros2 run my_bot slam_accuracy_check.py mark HOME --truth 0 0     # ต้องได้ ~0 = TF ปกติ
ros2 run my_bot slam_accuracy_check.py mark A --truth <x> <y>
ros2 run my_bot slam_accuracy_check.py mark B --truth <x> <y>
ros2 run my_bot slam_accuracy_check.py mark C --truth <x> <y>
ros2 run my_bot slam_accuracy_check.py mark HOME --truth 0 0     # ปิดรอบ
```

- วนแบบนี้ **อย่างน้อย 3 รอบ** · ขับ 0.10 m/s
- จอด**นิ่งสนิท**ก่อนรัน — ถ้าขยับเกิน 3 ซม. สคริปต์ไม่รับค่า
- **ห้ามปิด/รีสตาร์ท `make slam` กลางคัน** ถ้าต้องรีสตาร์ท เริ่มใหม่ทั้งชุดและย้ายไฟล์ผลออกก่อน
- จด error ที่สคริปต์พิมพ์ทุกครั้ง — ใช้ดูว่ารอบ 1, 2, 3 ใกล้กันไหม

```bash
ros2 run my_bot slam_accuracy_check.py --summary
```

### 5.4 อ่านผล — ต้องรายงานทั้ง 3 ค่า

| ค่า | คืออะไร |
|---|---|
| **ABSOLUTE** | error **แย่สุด**จากทุกครั้ง เทียบ 10 ซม. — ตัวเลขหลัก |
| **ALIGNED** | error หลังหักมุมเอียงระหว่างแนวเชือกกับแกนแผนที่ (วิธีเดียวกับ ATE ในงานวิจัย SLAM) |
| **REPEATABLE** | จอดจุดเดิมหลายรอบ ตำแหน่งต่างกันมากสุดเท่าไหร่ — ไม่มีตลับเมตรปน |

| เห็นอะไร | แปลว่า | ทำอะไร |
|---|---|---|
| ALIGNED ต่ำกว่า ABSOLUTE มาก | หุ่นหันเบี้ยวตอนเริ่ม ไม่ใช่ SLAM ผิด | รายงาน ALIGNED เป็นค่าหลัก **คู่กับ ABSOLUTE เสมอ** |
| `scale` ไม่ใช่ ~1.00 | รัศมีล้อ / ticks เพี้ยน | เขียนเป็นข้อสังเกต |
| error ไต่ขึ้นทุกรอบ | ขับเร็วไป / ล้อลื่น | ลดความเร็ว วัดใหม่ |
| ไม่ผ่านทั้งสองค่า | — | รายงานไม่ผ่าน พร้อมตัวเลขและสาเหตุ |

**เส้นที่ห้ามข้าม:** ห้ามตัดรอบที่แย่ทิ้ง · ห้ามแก้ตัววัตถุประสงค์หลังเห็นผล
เขียนเงื่อนไขทดสอบให้ครบ: ความเร็ว ขนาดพื้นที่ จำนวนจุด จำนวนรอบ

เขียนในรายงานด้วยว่า error นี้มีความคลาดเคลื่อนจากการจอดด้วยมือและตลับเมตรปนอยู่
จึงเป็น**ค่าสูงสุด** error จริงของ SLAM เท่ากับหรือดีกว่านี้

---

## 6. รอบ E — รูป Nav2

**เปิด:** `real` → `nav` → `rviz` · **ห้ามเปิด slam พร้อมกัน**

```bash
make nav
make rviz
```

ใน RViz กด **2D Goal Pose** → ลากไปจุดที่เส้นทางต้องเลี้ยว → แคปทันทีตอนเส้นสีเขียวขึ้น
และเห็นขอบสีฟ้า/ม่วงรอบสิ่งกีดขวาง → บันทึกเป็น `nav2_costmap_path.png`

---

## 7. งานที่โต๊ะ

### 7.1 ข้อ 2 — label และให้คะแนน (~3 ชม.)

```bash
cd ~/cap_ws/src/my_bot/scripts

# 1) ร่างกรอบด้วยโมเดลตัวใหญ่ (yolo26x.pt ดาวน์โหลดไว้ที่ ~/yolo/ แล้ว 24 ก.ย.)
#    ใช้ path เต็ม — ถ้าใส่แค่ yolo26x.pt จะโหลดใหม่ลงโฟลเดอร์ scripts/ ใน git (113 MB)
#    ห้ามใช้ yolo26s — เป็นตัวที่กำลังวัด สคริปต์จะไม่ยอมรัน
~/yolo/venv/bin/python detection_accuracy.py prelabel --data ~/eval/insitu2 \
    --model ~/yolo/yolo26x.pt --classes person chair backpack laptop

# 2) เปิด labelImg: โฟลเดอร์ภาพ = frames/ · save dir = labels_draft/
#    แก้ทุกภาพ: ลบกรอบผิด · เพิ่มกรอบที่ขาด · ขยับขอบให้พอดี
#    ตรวจครบทุกภาพแล้วค่อยย้าย:
mv ~/eval/insitu2/labels_draft/* ~/eval/insitu2/labels/

# 3) ให้คะแนน
python3 detection_accuracy.py score --data ~/eval/insitu2 \
    --pred ~/eval/insitu2/pred_onnx.json --run-label onnx-fp16 \
    --classes person chair backpack laptop \
    --figure ~/cap_ref/figures/accuracy.png
```

**กติกา label:**
- `classes.txt` ต้องสะกดตรงชื่อ COCO: `person` `chair` `backpack` `laptop` (prelabel เขียนให้แล้ว)
- ภาพที่ไม่มีวัตถุ ต้องมีไฟล์ `.txt` **เปล่า** — ถ้าไม่มีไฟล์ = ถูกข้าม
- ต้องตีกรอบ**ทุกชิ้น**ที่เห็น — เก้าอี้ที่ลืมตี ทำให้ผลที่ถูกกลายเป็นผิด

**อ่านผล `score`:**
- เกณฑ์คือ **macro F1 ≥ 80%** ที่ IoU 0.5 / conf 0.5 (ค่าเริ่มต้นของสคริปต์อยู่แล้ว)
- คลาสที่คอลัมน์ `truth` < 50 → `(< min, not averaged)` ไม่เข้าค่าเฉลี่ย แต่ยังรายงานจำนวน
- **ห้ามเปลี่ยนรายชื่อคลาสหรือ `--min-instances` หลังเห็นคะแนน**
- ถ้าเวลาไม่พอ ใช้ `--every 47` ตอน extract (~60 ภาพ) แล้วเขียนกำกับว่าใช้กี่ภาพ

**ต้องเขียนในบทที่ 5:**
- *"ภาพชุดทดสอบใช้เพื่อการประเมินเท่านั้น และได้รับความยินยอมจากบุคคลในภาพ"*
- *"ร่างกรอบเบื้องต้นด้วย yolo26x ซึ่งเป็นแบบจำลองคนละตัวกับที่ติดตั้งใช้งาน แล้วตรวจแก้ด้วยมือทุกภาพ"*
- bag อัดขณะเปิดเฉพาะตัวตรวจจับ (ไม่ได้เปิด SLAM)

### 7.2 วาดกราฟข้อ 1 และข้อ 5

```bash
cd ~/cap_ws/src/my_bot/scripts
python3 plot_objectives.py slam
python3 plot_objectives.py resource --window "full stack"
```

ได้ `slam_error_result.png` กับ `resource_usage.png` ที่ `~/cap_ref/figures/`
→ ก๊อปมาใส่ `CapstoneReport/images/`

ข้อ 4: `plot_objectives.py object` อ่านไฟล์ของสคริปต์ตัวเก่า ใช้กับผลใหม่ไม่ได้
→ รายงานเป็น**ตาราง 4 แถว**จาก `object_accuracy.py --summary` แทน

---

## 8. เอาผลไปใส่ตรงไหนในรายงาน

| ได้ตัวเลขจาก | ใส่ที่ |
|---|---|
| `slam_accuracy_check.py --summary` | `tab:eval` แถว 1 + รูป `slam_error_result.png` |
| `detection_accuracy.py score` | `tab:eval` แถว 2 + ตารางแยกคลาส + รูป `accuracy.png` (**ต้องเพิ่ม figure ใหม่**) |
| 13.05 FPS (มีแล้ว) | `tab:eval` แถว 3 ✅ |
| `object_accuracy.py --summary` | `tab:eval` แถว 4 + ตาราง 4 ด้าน |
| `resource_report.py --summary` | `tab:eval` แถว 5 + รูป `resource_usage.png` + ตารางแยกชั้น |

**ส่งมาให้ Claude:** ผล `--summary` ทุกตัว + ผล `score` + รูปทั้งหมด → เติมรายงาน แก้ข้อความที่ขัดกัน และคอมไพล์ให้

### เช็กลิสต์ก่อนส่ง

- [ ] `tab:eval` คอลัมน์ผลครบ 5 แถว — ข้อไหนไม่ผ่าน เขียนค่าจริงพร้อมเหตุผล **อย่าเว้นว่าง**
- [ ] แก้ `tab:eval` แถว 2 ช่องวิธีวัด ให้ตรงกับหัวข้อวิธีประเมิน (ภาพจากพื้นที่จริง · macro F1)
- [ ] ลบข้อความ "ยังไม่ได้วัด" ทุกจุด: บทคัดย่อ · สรุปผล · ย่อหน้าเหตุผลใต้ `tab:eval` · ข้อเสนอแนะ
- [ ] บทคัดย่อมีตัวเลขจริงครบ
- [ ] แทน `\imgph` ครบ 6 จุด

| รูป | ได้จากรอบ |
|---|---|
| `all_nodes_running.png` · `tf_tree.png` · `ros2_node_graph.png` | B |
| `nav2_costmap_path.png` | E |
| `slam_error_result.png` · `resource_usage.png` | โต๊ะ (หัวข้อ 7.2) |

- [ ] ก๊อปไฟล์ jsonl ทุกตัวจาก `~/maps/` เก็บเป็นหลักฐาน
- [ ] คอมไพล์รอบสุดท้าย ตรวจ `.log` ว่าไม่มี error และไม่มี undefined reference
