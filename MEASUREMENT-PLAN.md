# แผนการวัดวัตถุประสงค์ ฉบับใช้งานจริง

เขียนใหม่ 24 ก.ย. · แทนฉบับเดิมทั้งหมด · เน้นข้อ 4 ที่เปลี่ยนวิธีวัดแล้ว

---

## 0. สรุปภาพรวม — อ่านหน้านี้หน้าเดียวก็พอ

| ข้อ | วัดอะไร | เกณฑ์ | สถานะ | ต้องทำอะไรอีก | เวลา |
|---|---|---|---|---|---|
| 1 | ความคลาดเคลื่อนตำแหน่งบนแผนที่ | ≤ 10 ซม. | ❌ ยังเกิน | ดู `--summary` ก่อน แล้วเลือกทาง | 1 นาที → 40 นาที |
| 2 | ความแม่นยำการตรวจจับวัตถุ | ≥ 80% | ⬜ ยังไม่วัด | label ภาพจาก bag ที่มีอยู่ | 3 ชม. (ที่โต๊ะ) |
| 3 | ความเร็วประมวลผล | ≥ 10 FPS | ✅ **ผ่าน** 13.05 Hz | ไม่ต้องทำอะไร | 0 |
| 4 | ความคลาดเคลื่อนตำแหน่งวัตถุ | ≤ 50 ซม. | 🟡 ผ่านบางส่วน 8 ซม. | เก็บอีก 3 มุมมอง | **15 นาที** |
| 5 | CPU ขณะทำงานเต็มระบบ | ≤ 80% | ⬜ ยังไม่วัด | รันทิ้งไว้ระหว่างขับ | 0 (ทำพร้อมข้ออื่น) |

**ลำดับที่แนะนำ:** ข้อ 1 `--summary` (1 นาที) → ออกสนามทำข้อ 4 + ข้อ 5 + ข้อ 1 พร้อมกัน → กลับมาทำข้อ 2 ที่โต๊ะ

**รูปที่ยังขาด 6 รูป อยู่หัวข้อ 1 ถัดไปนี้เลย** — ส่วนใหญ่แคปได้ระหว่างออกสนาม

**งานคุ้มที่สุดของวันนี้คือข้อ 4** — 15 นาที เปลี่ยน "ผ่านบางส่วน" เป็น "ผ่าน" เต็มตัว

---

## 1. รูปที่ยังขาด 6 รูป — ได้มายังไง

ใส่แล้ว: `robot_overview.jpg`, `test_area.jpg` · ตัดออกแล้ว: `rviz_slam.png`, `detection_result.jpg`, `ekf_slip_comparison.png`

**ทุกรูปบันทึกลง `CapstoneReport/images/` ด้วยชื่อไฟล์ตามตารางเป๊ะ ๆ**

### ตารางสรุป — ดูแค่นี้ก็ทำได้

| # | ไฟล์ | บท | ต้องเปิดอะไรไว้ | คำสั่ง / วิธี | เวลา |
|---|---|---|---|---|---|
| 1 | `all_nodes_running.png` | ภาคผนวก | ระบบครบ (`real` `slam` `yolo` `semantic`) | `ros2 node list` → แคป terminal | 1 นาที |
| 2 | `tf_tree.png` | 4 | ระบบครบ | `ros2 run tf2_tools view_frames` → `pdftoppm -png -r 200 -singlefile frames_*.pdf tf_tree` | 1 นาที |
| 3 | `ros2_node_graph.png` | 3 | ระบบครบ | `rqt_graph` → ตั้งค่า 3 อย่าง (ด้านล่าง) → Save as image | 3 นาที |
| 4 | `nav2_costmap_path.png` | 4 | `real` + `nav` + `rviz` (**ไม่ใช่ slam**) | RViz กด **2D Goal Pose** → แคปตอนเส้นทางขึ้น | 5 นาที |
| 5 | `slam_error_result.png` | 5 | ไม่ต้อง (หลังวัดข้อ 1 เสร็จ) | `python3 plot_objectives.py slam` | 1 นาที |
| 6 | `resource_usage.png` | 5 | ไม่ต้อง (หลังวัดข้อ 5 เสร็จ) | `python3 plot_objectives.py resource --window "full stack"` | 1 นาที |

**ลำดับที่ประหยัดเวลาที่สุด:** ตอนระบบเปิดครบเพื่อวัดข้อ 4 ให้แคป #1 #2 #3 ต่อเลย →
วัดข้อ 1/4/5 เสร็จ ปิด `make slam` แล้วเปิด `make nav` ทำ #4 → กลับโต๊ะรัน #5 #6

---

### รายละเอียดทีละรูป

#### กลุ่ม ก — แคปตอนระบบเปิดครบ (ทำระหว่างออกสนาม ~10 นาที)

| ไฟล์ | อยู่บทไหน | วิธีได้มา |
|---|---|---|
| `all_nodes_running.png` | ภาคผนวก | เปิด terminal ใหม่ พิมพ์ `ros2 node list` แล้วแคปหน้าจอ terminal ทั้งหน้าต่าง |
| `tf_tree.png` | บทที่ 4 | ดูวิธีด้านล่าง |
| `ros2_node_graph.png` | บทที่ 3 | ดูวิธีด้านล่าง |
| `nav2_costmap_path.png` | บทที่ 4 | ดูวิธีด้านล่าง |

**`tf_tree.png`** — ต้องรันตอนระบบเปิดครบ ไม่งั้นต้นไม้จะขาดกิ่ง

```bash
cd ~ && ros2 run tf2_tools view_frames          # รอ 5 วินาที ได้ไฟล์ frames_<วันที่>.pdf
pdftoppm -png -r 200 -singlefile frames_*.pdf tf_tree   # ได้ tf_tree.png
```

ถ้าไม่มี `pdftoppm` ให้เปิด PDF แล้วแคปหน้าจอแทนก็ได้

ต้นไม้ที่ถูกต้องต้องมีสาย `map → odom → base_footprint → base_link` และเห็นเฟรมครบ: `chassis laser_frame camera_link imu_link left_wheel right_wheel`

**`ros2_node_graph.png`**

```bash
rqt_graph
```

ในหน้าต่างที่เปิดขึ้น ตั้งค่า 3 อย่างก่อนแคป:
1. ช่องซ้ายบนเลือก **Nodes/Topics (active)**
2. ติ๊ก **Hide: Dead sinks, Leaf topics, Debug, tf, Unreachable**
3. กดปุ่ม 🔄 refresh แล้วกดปุ่ม **Save as image** (ไอคอนรูปภาพมุมขวาบน) → บันทึกเป็น `ros2_node_graph.png`

ถ้ากราฟรกเกินอ่านไม่ออก ให้ติ๊ก Hide เพิ่มจนเหลือแต่เส้นหลัก: scan → slam → map, image → yolo → detections → semantic → landmarks

**`nav2_costmap_path.png`**

```bash
make nav        # แทน make slam ในรอบนี้ ใช้แผนที่ที่บันทึกไว้แล้ว
make rviz
```

ใน RViz กดปุ่ม **2D Goal Pose** แล้วคลิกลากไปจุดที่ไกลพอให้เส้นทางต้องเลี้ยว
**แคปทันทีหลังคลิก** ตอนเส้นทางสีเขียวขึ้นแล้ว และเห็นขอบสีฟ้า/ม่วงรอบสิ่งกีดขวาง (inflation)

> ⚠️ `make nav` ใช้แทน `make slam` ไม่ใช่เปิดพร้อมกัน — ทำรูปนี้**หลัง**วัดข้อ 1/4/5 เสร็จแล้ว

#### กลุ่ม ข — วาดจากผลการวัด (อัตโนมัติ 1 คำสั่ง)

| ไฟล์ | อยู่บทไหน | ได้หลังวัดข้อไหน |
|---|---|---|
| `slam_error_result.png` | บทที่ 5 | ข้อ 1 |
| `resource_usage.png` | บทที่ 5 | ข้อ 5 |

```bash
cd ~/cap_ws/src/my_bot/scripts
python3 plot_objectives.py slam
python3 plot_objectives.py resource --window "full stack"
# ได้ 2 ไฟล์ที่ ~/cap_ref/figures/ บน Jetson
```

แล้วก๊อป `slam_error_result.png` กับ `resource_usage.png` จาก Jetson มาใส่ `CapstoneReport/images/`

แต่ละคำสั่งพิมพ์ตัวเลขที่ใช้วาดออกมาด้วย — **ก๊อปตัวเลขชุดนั้นไปใส่ตาราง `tab:eval` ได้เลย** ตัวเลขกับรูปจะตรงกันแน่นอน

> ถ้าเลือก "ทาง ก" ของข้อ 1 (รายงานค่า aligned) รูปนี้ก็ยังใช้ได้ เพราะกราฟวาดทั้งค่า raw และ aligned

#### เช็กลิสต์รูป

- [ ] `all_nodes_running.png`
- [ ] `tf_tree.png`
- [ ] `ros2_node_graph.png`
- [ ] `nav2_costmap_path.png`
- [ ] `slam_error_result.png`
- [ ] `resource_usage.png`

#### เอารูปใส่รายงานยังไง

เปิด `project_report-good3.tex` ค้นคำว่า `\imgph` แล้วแทนทั้งบรรทัด เช่น

```latex
% เดิม
\imgph[0.85\textwidth]{6cm}{tf_tree.png}{ภาพต้นไม้ของระบบพิกัด...}
% ใหม่
\includegraphics[width=0.85\textwidth]{images/tf_tree.png}
```

แก้**แค่บรรทัด `\imgph` บรรทัดเดียว** — ไม่ต้องแตะ `\caption` หรือ `\label` ที่อยู่ข้างล่าง
หรือส่งรูปมาให้ผม เดี๋ยวใส่ให้แล้วคอมไพล์เช็กให้

---

## 2. เตรียมก่อนออกสนาม (ทำครั้งเดียว ~5 นาที)

เปิด 5 หน้าต่าง terminal บน Jetson ตามลำดับนี้ **ห้ามสลับลำดับ**

| # | คำสั่ง | รอจนเห็นอะไรถึงเปิดอันถัดไป |
|---|---|---|
| 1 | `cd ~/cap_ws && make real` | `controller_manager` ขึ้นครบ ไม่มี error สีแดง |
| 2 | `make slam` | ขึ้น `Registering sensor` แล้วนิ่ง |
| 3 | `make yolo` | ขึ้นเฟรมเรต ไหลต่อเนื่อง |
| 4 | `make semantic` | ขึ้น `semantic_mapper` พร้อมทำงาน |
| 5 | `make teleop` | ใช้ขับหุ่น |

แล้วเปิด terminal ที่ 6 ไว้สำหรับพิมพ์คำสั่งวัดผล

**เช็กว่าพร้อมจริงก่อนเริ่มวัด:**

```bash
ros2 topic hz /semantic_landmarks     # ต้องมีข้อมูลไหลออกมา
ros2 run tf2_ros tf2_echo map base_footprint   # ต้องไม่ error
```

ถ้าสองคำสั่งนี้ไม่ผ่าน อย่าเพิ่งเริ่มวัด — ย้อนไปดูว่า terminal ไหนตาย

---

## 3. ข้อ 4 — ความคลาดเคลื่อนตำแหน่งวัตถุ ≤ 50 ซม.

### 3.1 วิธีนี้ต่างจากเดิมยังไง

| | วิธีเดิม `landmark_tape_measure.py` | **วิธีใหม่ `object_accuracy.py`** |
|---|---|---|
| ต้องรู้พิกัด X Y ของวัตถุบนแผนที่ | ✅ ต้องรู้ | ❌ **ไม่ต้อง** |
| ต้องสำรวจจุด HOME จากข้อ 1 ก่อน | ✅ ต้อง | ❌ **ไม่ต้อง** |
| ถ้าย้ายวัตถุ | ต้องเริ่มชุดใหม่ทั้งหมด | วัดใหม่ได้เลย |
| วัดอะไรด้วยตลับเมตร | พิกัดวัตถุเทียบจุดอ้างอิงในห้อง | **ระยะจากหุ่นยนต์ถึงวัตถุ** |

วิธีใหม่ทำงานแบบนี้: อ่านตำแหน่งวัตถุที่ระบบบันทึกไว้บนแผนที่ แล้ว**แปลงกลับ**มาเป็น
"อยู่หน้าหุ่นกี่เมตร เยื้องซ้าย/ขวากี่เมตร" แล้วเทียบกับตลับเมตร
จึงไม่ต้องรู้พิกัดแผนที่ของอะไรเลย

### 3.2 ตัวเลข 2 ตัวที่ต้องวัดด้วยตลับเมตร

```
                    วัตถุ (เก้าอี้)
                      ●
                      |
                      |  ← left = ระยะตั้งฉาก
                      |    (ซ้าย = บวก, ขวา = ลบ)
      [หุ่นยนต์]------+------------→ แนวกึ่งกลางลำตัว
        ▲             
        |             
    กึ่งกลางเพลาล้อ   
        |←─ fwd = ระยะตามแนวกึ่งกลาง ─→|
```

**`--fwd`** วัดตามแนวกึ่งกลางลำตัวหุ่น จาก**กึ่งกลางเพลาล้อขับ** ไปจนถึงจุดที่ใกล้วัตถุที่สุด
**`--left`** วัดตั้งฉากจากจุดนั้นออกไปหาวัตถุ · วัตถุอยู่ซ้าย = ใส่ค่าบวก · อยู่ขวา = **ใส่ค่าลบ**

> ⚠️ **จุดเริ่มตลับเมตรคือกึ่งกลางเพลาล้อ ไม่ใช่หัวหุ่น**
> เพราะ `base_footprint` อยู่ตรงนั้น ถ้าวัดจากหัวหุ่น ค่าจะเกินไปทั้งหมด
> **ติดเทปทำเครื่องหมายไว้ที่ตัวหุ่นตรงกึ่งกลางเพลาเลย** จะได้ไม่ต้องกะทุกรอบ

> ⚠️ **หาแนวกึ่งกลางอย่าใช้สายตากะ** — หาจุดอ้างอิงบนตัวหุ่น 2 จุดที่อยู่บนแนวกึ่งกลาง
> แล้วขึงเชือกทาบยืดออกไปบนพื้น เอียง 3° ที่ 1.5 ม. คลาดไป 8 ซม. แล้ว

**ถ้าวัตถุอยู่เฉียงมากจนวัดยาก** วัดระยะตรงถึงวัตถุ (d) กับมุมเทียบแกนกลาง (θ) แล้วคิด:
`fwd = d × cos θ` และ `left = d × sin θ` (ซ้ายบวก)

### 3.3 ขั้นตอนวัด — 4 มุมมอง

เลือกวัตถุ 1 ชิ้นที่ **ไม่ขยับตลอดการวัด** (เก้าอี้ดีที่สุด — อยู่ในระนาบเลเซอร์พอดี)

| รอบ | จอดหุ่นยังไง | `--pass-label` |
|---|---|---|
| 1 | หันหน้าเข้าหาวัตถุจากด้านหน้า ห่าง ~1.5 ม. | `front` |
| 2 | ขับอ้อมไปด้านขวาของวัตถุ หันหน้าเข้าหา | `right` |
| 3 | ขับอ้อมไปด้านหลัง หันหน้าเข้าหา | `back` |
| 4 | ขับอ้อมไปด้านซ้าย หันหน้าเข้าหา | `left` |

**แต่ละรอบทำ 4 ขั้น:**

1. ขับไปจอด แล้ว**ปล่อยปุ่มให้หุ่นหยุดนิ่งสนิท** รอ 3 วินาที
2. วัดตลับเมตร → ได้ `fwd` กับ `left` ของรอบนั้น (**ค่าไม่เท่ากันทุกรอบ เพราะจอดใหม่**)
3. ดูว่ากล้องเห็นวัตถุจริงใน RViz หรือหน้าจอ yolo
4. รันคำสั่ง แล้ว**อย่าขยับหุ่นระหว่างรัน 30 วินาที**

```bash
ros2 run my_bot object_accuracy.py chair --fwd 1.52 --left -0.48 --pass-label front
ros2 run my_bot object_accuracy.py chair --fwd 1.38 --left  0.21 --pass-label right
ros2 run my_bot object_accuracy.py chair --fwd 1.61 --left  0.05 --pass-label back
ros2 run my_bot object_accuracy.py chair --fwd 1.44 --left -0.33 --pass-label left
```

> ตัวเลขข้างบนเป็น**ตัวอย่าง** ต้องแทนด้วยค่าที่วัดได้จริงของแต่ละรอบ

### 3.4 อ่านผลยังไง

แต่ละรอบจะพิมพ์แบบนี้:

```
=== chair · pass 'front' ===
  tape says          fwd +1.520  left -0.480 m     ← ที่คุณวัด
  system says        fwd +1.478  left -0.443 m     ← ที่ระบบคิด
  ERROR              5.6 cm      (criterion 50 cm) ← ตัวเลขที่เอาไปใส่รายงาน
  spread in window   1.2 cm over 28 samples        ← ความนิ่งขณะจอด
  landmark map pos   (+2.314, -0.907)   id a3f21b08

  PASS
```

**ดู 2 บรรทัด:** `ERROR` ต้อง ≤ 50 ซม. · `spread` ยิ่งน้อยยิ่งดี (บอกว่าค่าไม่แกว่ง)

**ถ้าเห็นบรรทัดขึ้นต้นด้วย `!`** แปลว่าระบบสร้าง landmark ซ้ำซ้อนของวัตถุเดียวกัน — จดไว้ เป็นข้อจำกัดที่ต้องเขียนในรายงาน

### 3.5 ดูผลรวมทั้ง 4 รอบ

```bash
ros2 run my_bot object_accuracy.py chair --summary
```

จะได้ตารางเทียบ 4 รอบ + **"การกระจายตัวระหว่างมุมมอง"** ซึ่งเป็นตัวเลขที่ขาดไป

สคริปต์จะบอกให้เองว่าการกระจายนั้นหมายถึงอะไร:

| ถ้าสคริปต์บอกว่า | แปลว่า | เขียนในรายงานว่า |
|---|---|---|
| **CONVERGENCE** (landmark id เดิม) | เป็นวัตถุชิ้นเดิม ตำแหน่งค่อย ๆ ปรับเข้าที่เมื่อเห็นจากหลายมุม | ระบบผสานข้อมูลข้ามมุมมองได้จริง |
| **VIEWPOINT DEPENDENCE** (id เปลี่ยน) | ระบบสร้างวัตถุใหม่แทนที่จะผสานกับของเดิม | เป็นข้อจำกัดที่ต้องรายงานตรง ๆ |

### 3.6 ปัญหาที่เจอบ่อย

| อาการ | สาเหตุ | แก้ |
|---|---|---|
| `no landmark of class 'chair'` | กล้องไม่เห็น / วัตถุสูงกว่าระนาบเลเซอร์ | ขับเข้าใกล้ขึ้น เช็กว่า yolo เห็นจริง |
| ERROR ใหญ่ผิดปกติ > 1 ม. | เผลอใส่ `--left` สลับเครื่องหมาย | ขวาต้องเป็นลบ |
| ERROR โตขึ้นเรื่อย ๆ ทุกรอบ | ข้อ 1 ยังคลาด ทำให้ตำแหน่งหุ่นบนแผนที่เพี้ยน | เป็นผลพวงของข้อ 1 ไม่ใช่ข้อ 4 — จดไว้แล้ววิเคราะห์ |
| `spread` ใหญ่ทั้งที่จอดนิ่ง | หุ่นยังไม่หยุดสนิท / วัตถุใกล้ขอบภาพ | รอเพิ่ม 3 วินาที แล้วรันใหม่ |

### 3.7 ⚠️ เรื่องกราฟข้อ 4

`plot_objectives.py object` **อ่านไฟล์ของสคริปต์ตัวเก่า** (`~/maps/tape_session.jsonl`)
ส่วนสคริปต์ใหม่เขียนลง `~/maps/object_accuracy.jsonl` ซึ่งรูปแบบข้อมูลคนละแบบ

เลือกทางใดทางหนึ่ง:
- **ทาง ก (แนะนำ)** ใช้สคริปต์ใหม่ แล้วรายงานเป็น**ตาราง 4 แถว**จาก `--summary` แทนกราฟ — ตัวเลขครบเหมือนกัน
- **ทาง ข** ถ้าอยากได้กราฟจริง ๆ บอกมา เดี๋ยวเพิ่มตัวเลือก `--figure` ให้สคริปต์ใหม่วาดเอง

---

## 4. ข้อ 1 — ความคลาดเคลื่อนแผนที่ ≤ 10 ซม.

### ทำก่อนอื่นเลย 30 วินาที

```bash
ros2 run my_bot slam_accuracy_check.py --summary
```

**เข้าใจก่อน:** สคริปต์นี้อ่าน `map → base_footprint` อยู่แล้ว คือตำแหน่งที่ไลดาร์แก้แล้ว
**ไม่ใช่ odom ดิบ** เพราะงั้น "เปลี่ยนไปยึดไลดาร์แทน odom" คือสิ่งที่ทำอยู่แล้วตั้งแต่แรก

ดู 3 ตัวเลขจาก `--summary` แล้วเลือกทาง:

| เห็นอะไร | แปลว่า | ไปทางไหน |
|---|---|---|
| `aligned` ต่ำกว่า `raw` มาก | แกนตลับเมตรเอียงจากแกนแผนที่ | **ทาง ก** |
| `scale` ไม่ใช่ ~1.00 | รัศมีล้อ/ticks เพี้ยน | **ทาง ข** |
| raw กับ aligned สูงพอกัน และ scale ≈ 1 | แผนที่บิดจริง | **ทาง ข** |

### ทาง ก — รายงานค่า aligned เป็นค่าหลัก (ไม่ต้องวัดใหม่)

ค่า aligned คือความคลาดเคลื่อนเชิงตำแหน่งเหมือนกัน แค่ตัดการหมุนของเฟรมอ้างอิงที่เรากำหนดเองออก
เป็นวิธีมาตรฐานเดียวกับที่ benchmark SLAM ทุกตัวใช้ (ATE)

> ความคลาดเคลื่อนเชิงตำแหน่งหลังชดเชยการหมุนของเฟรมอ้างอิง อันเป็นวิธีมาตรฐานเดียวกับ
> การวัด ATE เท่ากับ \_\_ ซม. ส่วนค่าก่อนชดเชยเท่ากับ \_\_ ซม. ผลต่างมาจากมุม \_\_ องศา
> ระหว่างแกนที่ใช้วัดด้วยตลับเมตรกับแกนที่ slam\_toolbox กำหนด ซึ่งเป็นความคลาดเคลื่อน
> ของขั้นตอนสำรวจจุดอ้างอิง ไม่ใช่ของกระบวนการสร้างแผนที่

### ทาง ข — วัดใหม่ ~40 นาที แก้ 2 อย่าง

1. **ขับช้าลงเหลือ 0.10 m/s** ด้วย `make teleop SPEED=0.10` — ตั้งแต่ D-26 เพดานเป็น 0.30 แล้วไม่มีอะไรบังคับ
   ที่ 0.30 สแกนเบี้ยว 2.6 ซม./รอบ กินงบ 10 ซม. ไปหนึ่งในสาม
2. **จัดแกนให้ตรงก่อน `make slam`** — ขึงเชือกยาว 3–4 ม. วางแนวกลางหุ่นทาบเส้นนั้น
   แล้ววัดพิกัดจุดอ้างอิงเทียบ**เส้นนั้น** ไม่ใช่เทียบผนัง

### เสริม 5 นาที — ความถูกต้องเชิงขนาด

วัดตลับเมตร 3 ระยะ (กว้าง ยาว ทแยง) **ข้ามห้อง** แล้วเทียบกับ `.pgm` โดยนับพิกเซล × 0.05 ม.

> อย่าวัดจากหุ่นถึงกำแพงใกล้ ๆ — SLAM วางหุ่นตรงที่ทำให้สแกนทาบกำแพงพอดีอยู่แล้ว ค่าจะตรงเสมอ
> และต้องเขียนกำกับว่าแผนที่ละเอียด 0.05 ม./ช่อง จึงมีความไม่แน่นอน ±5 ซม. ที่ปลายแต่ละด้าน

### ถ้า error มากจริงและแก้ไม่ทัน — ไม่ต้องแก้วัตถุประสงค์

วัตถุประสงค์ข้อ 1 ในรายงานเขียนว่า *"ระบบต้องมีความคลาดเคลื่อนเชิงตำแหน่งไม่เกิน 10 เซนติเมตร
เมื่อทดสอบในพื้นที่ควบคุมขนาดไม่น้อยกว่า 5×5 เมตร"* — **ไม่ได้ระบุว่าต้องวัดด้วยวิธีไหน**
จึงมีวิธีวัดที่ถูกต้องตามหลักวิชาการหลายแบบ เรียงจากง่ายไปยาก:

| # | วิธี | ต้องวัดใหม่ไหม | ทำไมใช้ได้ |
|---|---|---|---|
| 1 | **ค่า aligned (ATE)** เป็นค่าหลัก — ทาง ก | ไม่ต้อง | วิธีมาตรฐานของงานวิจัย SLAM ตัดมุมเอียงของแกนตลับเมตรออก |
| 2 | **ความถูกต้องของตัวแผนที่** — หัวข้อเสริมข้างบน | 5 นาที | วัดตำแหน่งผนังบนแผนที่ตรง ๆ ตรงกับคำว่า "สร้างแผนที่เชิงพื้นที่" |
| 3 | **กำหนดเงื่อนไขทดสอบ** 0.10 m/s — ทาง ข | ~40 นาที | "พื้นที่ควบคุม" เปิดให้กำหนดวิธีทดสอบได้ ไม่ใช่การแก้วัตถุประสงค์ |
| 4 | **รายงานว่าไม่ผ่าน** พร้อมตัวเลข สาเหตุ แนวทางแก้ | ไม่ต้อง | ไม่ต้องผ่านทุกข้อ — rubric PI 8.2/10.2 ให้คะแนนการรายงานครบและการวิเคราะห์ |

**ลองข้อ 1 ก่อนเสมอ** — ถ้า raw สูงแต่ aligned ต่ำ ส่วนต่างมาจากการวางแกนตอนวัด ไม่ใช่จากแผนที่

**อย่าแก้วัตถุประสงค์หลังทดสอบ** — กรรมการจะเห็นว่าเป็นการย้ายเส้นชัย แย่กว่ารายงานว่าไม่ผ่านตรง ๆ

**เส้นที่ห้ามข้าม ไม่ว่าใช้วิธีไหน:**
- ใช้ค่า aligned ได้ แต่ **ต้องแสดงค่า raw คู่กันเสมอ**
- **ห้ามเลือกเฉพาะรอบที่ผ่าน** หรือตัดจุดที่คลาดมากทิ้ง
- เขียนวิธีวัดในบทที่ 5 ให้ชัด: วัดอะไร เทียบกับอะไร เงื่อนไขอะไร (ความเร็ว ขนาดพื้นที่ จำนวนจุด)

ทำครบตามนี้ แม้ใช้ตัวชี้วัดต่างจากที่ตั้งใจแต่แรก กรรมการก็ตรวจสอบและยอมรับได้

---

## 5. ข้อ 5 — CPU ≤ 80%

เปิดทิ้งไว้ระหว่างที่ออกสนามทำข้ออื่น **ไม่เสียเวลาเพิ่ม**

```bash
ros2 run my_bot resource_report.py --seconds 1800 --label "full stack, driving"
```

รันตอนระบบเปิดครบทุก terminal แล้ว ก่อนเริ่มขับ — ปล่อยไว้ตลอดรอบ

**ของเสริม ถ้าอยากได้กราฟเปรียบเทียบ** (ข้ามได้ ไม่กระทบเกณฑ์):

```bash
ros2 run my_bot resource_report.py --seconds 120 --label "idle"        # ก่อนเปิดอะไร
ros2 run my_bot resource_report.py --seconds 180 --label "real+slam"   # ยังไม่เปิด yolo
```

---

## 6. ข้อ 2 — ความแม่นยำการตรวจจับ ≥ 80%

**ทำที่โต๊ะ ไม่ต้องใช้หุ่นยนต์** ใช้ bag ที่มีอยู่แล้ว `~/bags/2026-09-22-163401`

```bash
cd ~/cap_ws/src/my_bot/scripts

# 1) ดึงภาพออกมา — คำสั่งนี้พิมพ์จำนวน instance ต่อคลาสให้ดูด้วย
python3 detection_accuracy.py extract \
    --bag ~/bags/2026-09-22-163401 --out ~/eval/insitu --every 23

# 2) label ~120 เฟรมลง ~/eval/insitu/labels/ (YOLO txt + classes.txt)
#    เฟรมที่ไม่มีวัตถุเป้าหมาย ต้องมีไฟล์ .txt เปล่า ไม่ใช่ไม่มีไฟล์

# 3) ให้คะแนน
python3 detection_accuracy.py score --data ~/eval/insitu \
    --pred ~/eval/insitu/pred_onnx.json --run-label onnx-fp16 \
    --classes person chair bench laptop \
    --figure ~/cap_ref/figures/accuracy.png
```

- **คลาสเป้าหมาย:** `person chair bench laptop` (ประกาศไว้ในรายงานแล้ว)
- **เกณฑ์ตัดสินคือ macro F1** ที่ IoU 0.5 / conf 0.5 — คลาสที่มีน้อยกว่า 50 instance ไม่นับ
- **ถ้าเวลาไม่พอ:** ใช้ `--every 47` เหลือ ~60 เฟรม แล้ว**เขียนกำกับว่าใช้กี่เฟรม**
- **การเทียบ .pt กับ .onnx เป็นของเสริม** ข้ามได้ ตัวเลข ONNX อย่างเดียวตอบเกณฑ์ได้แล้ว

---

## 7. ข้อ 3 — เสร็จแล้ว ✅

13.05 Hz ขณะ SLAM ทำงาน · 15.13 Hz เมื่อทำงานลำพัง · รูป `model_accuracy_fps.png` อยู่ในรายงานแล้ว
**ไม่ต้องทำอะไรเพิ่ม**

---

## 8. เอาตัวเลขไปใส่ตรงไหนในรายงาน

| ได้ตัวเลขจาก | เอาไปใส่ |
|---|---|
| `slam_accuracy_check.py --summary` | ตาราง `tab:eval` แถวที่ 1 + บทที่ 5 |
| `detection_accuracy.py score` | `tab:eval` แถวที่ 2 + รูป `accuracy.png` |
| ของเดิม 13.05 Hz | `tab:eval` แถวที่ 3 ✅ |
| `object_accuracy.py --summary` | `tab:eval` แถวที่ 4 + ตาราง 4 มุมมอง |
| `resource_report.py` | `tab:eval` แถวที่ 5 |

**ก่อนส่ง:**
- [ ] เติมคอลัมน์ **ผล** ใน `tab:eval` ครบทั้ง 5 แถว — ข้อไหนไม่ผ่านให้เขียนค่าจริงพร้อมเหตุผล **อย่าเว้นว่าง**
- [ ] ระบุในคำบรรยายตารางว่าวัดจากคอนฟิกไหน (เปิดหรือปิดการผสานข้อมูล)
- [ ] อัปเดตบทคัดย่อให้มีตัวเลขจริง
- [ ] แทน `\imgph` ที่เหลือ 6 จุด (ดูหัวข้อ 1)
- [ ] คอมไพล์รอบสุดท้าย ตรวจ `.log` ว่าไม่มี error และไม่มี undefined reference
